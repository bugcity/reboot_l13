[project]
name = "reboot-l13"
version = "0.1.1"
description = "reboot ZTE L13"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "requests>=2.32.3",
]

[project.scripts]
reboot-l13 = "reboot_l13:main"

[tool.setuptools.packages.find]
where = ["src"]
